# Same allowlist again, in the form older pnpm (10.0-10.5) reads. Unknown keys
# are ignored by npm, so this is inert outside a pnpm build.
only-built-dependencies[]=sharp
only-built-dependencies[]=esbuild
