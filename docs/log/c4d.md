# C4d - Bounded translation runs

- Landed locally: DeepL and Gemini fetches each use a 30000 ms abort signal; each Claude call creates one Anthropic client with timeout 45000 ms and maxRetries 1. The translation runner defaults to 50 attempts (raise with `--limit`), counts failures toward the cap, and breaks the generator at the cap in both dry and real runs. Further candidates are explicitly not scanned; no postponed total is claimed. No existing postponed-count helper was available or required. Hash logic, provider order, schema and other jobs are unchanged.
- Verification: `npm run typecheck` PASS. `npm run cron:translate -- --dry --limit 2` FAIL before database access: `SystemError [ERR_SYSTEM_ERROR]: A system error occurred: uv_os_get_passwd returned ENOMEM (not enough memory)` (Node.js v24.16.0, tsx startup). Not retried. Both commands also warned about npm's unknown `only-built-dependencies` project config.
- Not verified: database-backed cap behavior, live provider deadlines/retries, or production process counts. No provider calls, build, Git commands or deployment. Provider deadlines and the attempt cap do not impose a whole-job deadline on database waits.
- Deviations: no implementation scope deviations; the dry-run check was blocked by the runtime startup error above.
- Founder action (audit candidate 1): update the hPanel cron schedule/commands to stagger jobs and apply a nonblocking per-job lock plus a whole-process-tree runtime limit if supported, preserving FX-before-cuotas order and bounded translation/image batches. hPanel was not inspected or changed.
