# P5 — docs truth pass

Phase P5 of `fable-plan-polish.md`. Branch `claude/p5-docs-truth`. Docs only.

## What was actually checked

- **Owner inbox (D8).** Read `app/mis-avisos/{page,tabs,actions}.tsx`,
  `app/mis-avisos/consultas/page.tsx` and `app/api/leads/route.ts`'s
  `routedTo` chain. The inbox is built and live: `/mis-avisos` (list, status
  change, edit link) and `/mis-avisos/consultas` (leads, WhatsApp reply),
  both scoped by `requireOwnerContext()`, and `routed_to` already has an
  `owner` lane. CLAUDE.md backlog item 8 and PLAN.md's D8 line both said
  otherwise — rewritten to say it is done, with the one real gap: nothing
  notifies the owner of a new lead (they have to open the page themselves).
- **Directory door rows.** `src/config/verticals.ts` shows `inmobiliarios.com.py`
  `enabled: true, ownsDirectory: true`, DNS live per `docs/log/d1.md`/`d1b.md`.
  CLAUDE.md's domain table and backlog item 11 already reflected this
  (an earlier session had fixed them) — left as-is after verifying.
- **rentparaguay.com**: DNS live 2026-09-10 per the task brief — the row said
  "DNS pending", now corrected; WhatsApp env var and WordPress-redirect check
  stay open founder items.
- **alquiler.com.py**: not purchased. Row now says so explicitly, names
  `alquileres.com.py` as the fallback choice, and points at
  `fable-plan-polish.md`'s one-hour rename task instead of implying it is a
  DNS-only wait like its English pair.
- **README domain section** was pre-D6 stale (named `realestateinparaguay.com`
  primary). Rewritten to list all five doors with today's live/not-live state.
- **Reviews (backlog item 4)**: unchanged in CLAUDE.md; added one entry to
  `docs/decisions-needed.md` (new file) with three options, recommending (c).

## Known issues

None — docs only, no code touched.

## Verification

`npm run verify:i18n` green (no code touched, ran anyway per the exit list).
Every sentence above traces to a file read this session: `app/mis-avisos/**`,
`app/api/leads/route.ts`, `src/config/verticals.ts`, `docs/log/d1.md`,
`docs/log/d1b.md`, `PLAN.md` (D8, F4, Batch 2), `README.md`.
