# C0 - Editorial schema-drift fallback (F12)

Changed `src/lib/post-queries.ts` to recognise `ER_NO_SUCH_TABLE`,
`ER_BAD_FIELD_ERROR`, and missing-table/unknown-column/unknown-field messages
through the existing bounded error cause traversal. Driver codes take priority;
message-only failures use the corresponding inferred MySQL code.

All five existing fail-soft catches use the helper. Lists and sitemap reads
return empty arrays; detail (including related-query failures) returns null;
readiness returns false and draft count returns zero. Each handled failure logs
exactly one warning: `[posts] schema drift, degrading to empty: <code>`.
No error messages, SQL, or credentials are logged. Healthy queries and unrelated
error propagation are unchanged. Writes are unchanged.

## Verification

- PASS: `npm run typecheck` (also rerun after the code change).
- FAIL: `npm run verify:local`. Its typecheck passed, but Next.js build failed:
  `Error: EPERM: operation not permitted, readlink 'C:\Users\anton'` followed by
  `Build failed because of webpack errors`. Next.js reported multiple lockfiles
  and inferred `C:\Users\anton` as workspace root. The subsequent import,
  facets, i18n, and SEO checks were not reached.
- PASS: inline Node/TypeScript VM harness, 39 mocked read scenarios covering
  all five catches, driver codes, wrapped causes, message fallbacks, detail's
  related-query failure, healthy list/detail reads, one safe warning per failure,
  and unrelated-error propagation. Database and cache were mocked.
- PASS: `git diff --check`.
- Initial tooling attempts failed because `rg` and `python` were unavailable;
  PowerShell searches and patch tools completed the work instead.

## Not verified / release boundary

No real database or live HTTP verification was performed. Production migration
0014 remains a founder action; this fallback does not apply migrations. No env,
schema, connection, route, or build configuration was changed. No push or deploy.
Work stays on the requested current branch, `codex/c0-guias-failsoft`.

The requested staging and commit were attempted but both failed with
`fatal: Unable to create 'C:/Users/anton/propia.node/.git/index.lock': Permission denied`.
The sandbox permits reading `.git` but blocks writing it; no commit was created.
An unrelated untracked file, `prompts-site-quality/codex-c2-english-pages.md`,
appeared during the task and was left untouched.
