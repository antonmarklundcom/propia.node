# C2 - English door completeness (F8)

## Earlier pass, verified from the branch diff

Source: `git diff 9e23292^ dfba10f` (commits 9e23292 and dfba10f).

- Routed public about, contact, agents, agencies, guides, projects/project detail,
  prices/city prices, valuation and publishing pages through the dictionary.
- Localized public login/registration pages, PublishWizard and ValuationTool;
  added Spanish/English peers and exposed them through Dictionary.
- Made root metadata description/tagline and Open Graph locale request-aware.
  The follow-up made the home tab title use the serving vertical's locale.
- These earlier commits changed 20 files. Existing C1 styling changes in the
  broader branch diff are separate work and were not edited in this pass.

## This completion pass

- Agent detail metadata, profile labels and inquiry prefill use dict(). Added
  the missing English agent-profile namespace and English prefill functions.
- Property detail and category price context use the existing precios dictionary.
  English property context uses the translated property-type label. Property
  inquiry text uses the selected dictionary and the English title when available.
- PriceAlert requires the caller's locale and uses getDictionary(locale) for
  its trigger, form, completion text and lead message. Extracted its Spanish
  literals without changing their output; existing Spanish prefills are reused.
- SiteNotice uses dict().siteNotice; its existing English peer is reused.
- Dictionary now exposes agentProfile, siteNotice, priceAlert and both prefill
  helpers. No server helper was missing, so src/i18n/server.ts was not edited.
- No URLs, premium copy, styles, protected areas, schema or routing changed.

## Validation and limits

- PASS: npm run typecheck (tsc --noEmit).
- FAIL: npm run verify:i18n. Before checks execute, tsx fails with
  `SystemError [ERR_SYSTEM_ERROR]: A system error occurred: uv_os_get_passwd returned ENOMEM (not enough memory)`
  in `node_modules/tsx/dist/temporary-directory-BDDVQOvU.mjs`, Node.js v24.16.0.
- FAIL: npm run verify:seo. Same tsx startup error and runtime as above.
- PASS: recursive PowerShell Select-String scan for direct @/i18n/es imports:
  only admin, agencia, mis-avisos, app/api, components/panel and
  app/publicar/actions.ts remain. rg is unavailable in this shell.
- PASS: git diff --check (line-ending warnings only).
- git status --short: this pass changes only the nine authorized files including
  this log. Two pre-existing untracked files are untouched:
  docs/prompts/premium-editorial-inner.md and
  prompts-site-quality/codex-c3a-detail-page.md.
- Build not run, per task instruction concerning sandbox EPERM readlink failure.
  No production/browser or database validation performed. The two failed
  verification commands still need to pass outside this sandbox.
- No staging, commits, push, PR or deployment performed; director owns commits.
