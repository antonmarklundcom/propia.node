# C4 director log — 2026-09-14

Manager: Fable 5.1 (interactive). Worker: Codex CLI gpt-6-astra effort low for every dispatch. Branch `codex/c4-remaining`, fast-forwarded into `main`.

## Merge state at session start
`main` had only C0. #142 (C1) was squash-merged mid-session by Anton; the tip `codex/c3c-marketing-pages` was then merged on top by the director (one conflict in `app/globals.css`, the `.ph-featured .listing-card__nophoto` hunk, resolved to the tip's C3a values; merge result byte-identical to the tip). `verify:local` green before push.

## Dispatches
| Phase | Session | What | Audit |
|---|---|---|---|
| C4a | 01a0a00b-593a-74a1-abde-8be674ab6f3e | LeadForm editorial restyle + glyph success, ProjectCard glyph placeholder | typecheck, verify:i18n, render /contacto both doors |
| C4b | 01a0a00e-2d07-77f2-8a6e-4f5751f5feca | hub type chips, /precios index, prompt doc group (d) | typecheck, verify:i18n, render /venta + /precios |
| C4c | 01a0a012-cedc-7db0-9d93-d6f3ee77ca27 | /precios/[ciudad] restyle (was still admin-panel look) | typecheck, verify:i18n, render |
| C4d | 01a0a016-12be-7bb2-b645-34e2080684b8 | process-audit candidate 2: provider deadlines 30 s / 45 s, default cap 50 attempts, loop breaks at cap | typecheck; `cron:translate --dry --limit 2` on local DB stops at 2 |
| C4e | 01a0a01b-0fc0-7fc1-8f78-c3cfbe268091 | final emoji sweep: 9 new glyphs, 11 files, dictionary icons as glyph names | node emoji scan over app+src = 0 (remaining ✓ ✕ ☰ are typographic); render 11 routes |
| C4f | (log in codex-runs 2026-09-14) | /para-inmobiliarias moved to the dictionary; EN door now English there | typecheck, verify:i18n, 51/51 old Spanish strings byte-identical in es.ts, render both doors |

## Render results (local seeded MySQL, 47 listings, both doors, 1440 and 390)
- All public routes 200, `scrollWidth` == viewport on every route at both widths.
- Visible emoji: 0 on every route (the 390 hit is the ☰ menu glyph).
- Exactly one `form.lead-form` on /contacto and /para-inmobiliarias.
- /precios and /precios/asuncion: hero, list and method sections editorial. The medians table never renders with the seed data (no city/type group reaches 8 listings, same as prod today), so `.precios-table*` CSS is unverified by render.
- /precios index empty state still uses the dashed `.panel-empty` box (minor, cheap-tier follow-up).

## Not done / open
- Live audit against the deployed doors (`docs/log/live-2026-09-14.md`) not run: Hostinger does not auto-deploy, prod still serves the pre-C1 build.
- Process-audit candidate 1 is a founder hPanel action (stagger crons, per-job lock, runtime limit).
- `gh` auth still expired on this PC; everything merged by fast-forward push.
