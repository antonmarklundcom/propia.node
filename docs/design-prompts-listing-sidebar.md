# Prompt 1 — Listing and category sidebar

```text
You are Claude Design running Opus in a fresh session. Deliver a visual design and a concrete implementation plan; do not deploy, commit, or change production data.

Project: C:\Claude 1\propia.node. GitHub: https://github.com/antonmarklundcom/propia.node (branch main). Spanish live door: https://inmobiliaria.com.py. English door: https://realestateinparaguay.com. This is a Paraguayan real-estate portal for local buyers and foreign buyers. Stack: Next.js 15 App Router, React 19, TypeScript, MySQL 8 with drizzle. The repo does not use Tailwind: use its existing global CSS and components. No new dependencies.

Read first, in order: AGENTS.md; CLAUDE.md (Domains, Backlog state, Listing filters, i18n); app/globals.css (filter-bar, category, c3b styles); src/design/tokens.ts; src/design/themes.ts; src/design/sections.ts; app/[operacion]/page.tsx; app/[operacion]/[...segments]/page.tsx; src/components/CategoryFilterBar.tsx; src/components/ListingBrowser.tsx if present; src/components/ListingCard.tsx; src/lib/urls.ts; src/lib/facets.ts; src/lib/facet-sql.ts; src/lib/queries.ts; src/lib/indexability.ts; src/i18n/es.ts; src/i18n/en.ts; tests/e2e/sort.spec.ts and playwright.config.ts. Inspect local changes without resetting them.

Design system actually used by both marketplace doors: editorial deep green --color-primary #0E1F17, primary-dark #0B1710, primary-soft #12271D; gold --color-accent #C19A4D and hover #DDBC7C; cream --color-background #F6F3EC; ink #16211B, secondary ink #56605A, border rgba(22,33,27,0.12). Body links use --color-link #8A6626, never the low-contrast gold for body text on cream. Display font is self-hosted Cormorant Garamond; body is self-hosted Jost. Use --font-display and --font-sans. Editorial controls and cards have zero radius, no soft floating shadows, and photo cards use a gradient with overlaid text. Respect host theme overrides via CSS variables. The default container is 1440px, header 76px, grid gap clamp(12px,1.6vw,24px). Gold is an accent, not a full section background.

Original verified problems: /venta, /alquiler and /alquiler-temporal were hub pages showing only about eight recent listings plus city links, ignored precio_min, precio_max, dormitorios and orden, and had no filter controls. Categories such as /venta/asuncion did filter, but only after a submit and with no pending feedback. Cards display their native US$ or Gs currency while filters and sort use price_usd. Stored price_usd on Gs listings may use an older exchange rate: flag this, never change money logic. A sidebar implementation may now exist locally; assess it before planning a replacement.

Scope: design desktop sticky left sidebar and mobile Filtros drawer, then results toolbar, cards grid, pagination, empty and loading states. Sidebar order: result count, clearly labelled USD min/max with four presets, 1+/2+/3+/4+ bedroom tags, all existing property types, cities on hubs and barrios on city pages. Optional future bathrooms/parking/minimum area require the shared facet layer. Toolbar: count, recientes/precio_asc/precio_desc, list/map switch, removable chips and clear all. Preserve hub editorial content and metadata while showing all operation listings through the paged shared query.

Hard constraints: all visitor copy belongs in src/i18n/es.ts and en.ts together, accessed through dict() or getDictionary(locale); no inline strings or invented claims. English should help foreign buyers. Format numbers with es-PY/en-US. URLs are the source of truth; keep existing Spanish parameter names and canonical paths. Filters become SQL only through facets.ts/facet-sql.ts. Vertical filters must AND with visitor choices; caches include the vertical key. Retain native GET form functionality without JavaScript. Mobile uses Apply; pending navigation needs a visible spinner and dimmed/skeleton results. Dialog must trap focus, close with Escape and restore focus. Mobile first, usable at 320px, labelled controls, keyboard navigation, visible focus, live result count, reduced-motion support. Do not edit schema, migrations, auth, leads, notifications, exchange rates, dependencies, next.config.ts or SEO ownership fields. Filtered/sorted URLs use a clean canonical and noindex,follow; canonical category inventory thresholds stay independent of filters.

Deliver annotated 320px, 768px and 1440px designs with populated, empty, pending, active-filter and open-drawer states, bilingual copy references, component/data-flow plan, exact file changes and acceptance tests. Success: hub and category filters change real cards and count, chips remove one constraint, sort/map/pagination preserve choices, no-JS GET works, drawer is keyboard safe, no overflow, no new indexable filter URLs. Validate against tests/e2e and the four commands npm run typecheck, npm run verify:facets, npm run verify:i18n, npm run verify:seo. Do not run next build or verify:local in the constrained sandbox; report unverified runtime checks honestly.
```

# Prompt 2 — Property cards and search entry points

Chosen because clearer cards and consistent home/hero search help visitors reach relevant inventory and qualified enquiries with less effort.

```text
You are Claude Design running Opus in a fresh session. Deliver a visual design plus an implementation plan for property cards and the home/hub search entry points. Do not implement production changes or modify the lead pipeline.

Local project: C:\Claude 1\propia.node. GitHub repo: https://github.com/antonmarklundcom/propia.node (branch main). Live Spanish site: https://inmobiliaria.com.py. English door for foreign buyers: https://realestateinparaguay.com. Stack: Next.js 15 App Router, React 19, TypeScript, MySQL 8/drizzle. There is no Tailwind in this repo; use the existing CSS system. No new dependencies.

Read first: AGENTS.md; CLAUDE.md (Domains, Backlog state, Listing filters, i18n); app/globals.css; src/design/tokens.ts; src/design/themes.ts; src/design/sections.ts; app/page.tsx; app/[operacion]/page.tsx; app/[operacion]/[...segments]/page.tsx; src/components/ListingCard.tsx; src/components/SearchBar.tsx; src/components/CategoryFilterBar.tsx; src/components/ListingBrowser.tsx if present; src/lib/urls.ts; src/lib/facets.ts; src/lib/facet-sql.ts; src/lib/format.ts; src/lib/queries.ts; src/i18n/es.ts; src/i18n/en.ts. Follow imports from the home page into src/design to find its actual section components. Do not reset local work.

Actual marketplace tokens: deep green primary #0E1F17, primary-dark #0B1710, primary-soft #12271D; gold accent #C19A4D, accent-hover #DDBC7C; cream background #F6F3EC; ink #16211B, secondary #56605A; border rgba(22,33,27,0.12). Body links on cream use #8A6626, not low-contrast accent gold. Typography: self-hosted Cormorant Garamond display and Jost body, reached via --font-display/--font-sans. Zero-radius editorial cards/controls, no floating soft shadows, gradient photo cards with text overlays, contained gold accents. Default --container 1440px, --header-h 76px, --grid-gap clamp(12px,1.6vw,24px). Respect per-domain CSS variable overrides.

Context and original problems: operation hubs /venta, /alquiler, /alquiler-temporal showed about eight recent listings and city links without filters; precio_min, precio_max, dormitorios and orden were ignored there. Category pages such as /venta/asuncion filtered correctly only after submission, without loading feedback. A shared sidebar/results experience is being added; work with its URL contract instead of creating a separate search system. Cards show US$ or Gs natively, but price filtering and sorting use price_usd. Existing Gs price_usd may reflect an older exchange rate: flag this concern; never recompute or invent rates.

Different scope from the listing-sidebar design: improve the reusable property card and the search journey from home and hub heroes into existing listing results. Explore a legible price/location/facts hierarchy, explicit currency, honest missing-image and missing-data states, existing verification badges only when supported by actual data, long bilingual titles, photo contrast, click targets and stable image loading. Preserve title_en fallback behavior and the distinction between an agent verification badge and property verification. Do not invent legal guarantees, listing freshness, reviews or financing offers. Search should clearly retain operation, city and type, including temporary rentals where supported, and land on existing canonical categories or operation hubs. Show how a foreign buyer and a local mobile buyer each reach an affordable relevant shortlist. Do not design a new lead form or alter auth, notifications or lead code.

Constraints: copy lives in src/i18n/es.ts and en.ts, added together through dict()/getDictionary(locale); no inline visitor strings. English expresses the intent for foreign buyers without inventing facts. Numbers use es-PY/en-US. No schema, migration, dependency, next.config.ts, domain/brand ownership, price_usd or exchange-rate changes. All narrowing uses src/lib/facets.ts and src/lib/facet-sql.ts; vertical restrictions only AND with user choices and cached results include the vertical key. Preserve canonical URLs and noindex rules for transient filter/sort URLs. Maintain GET progressive enhancement and the sidebar's parameter names. Mobile first at 320px; keyboard-operable controls, visible focus, accessible labels, sufficient text contrast, sensible reading order, reduced-motion support, no layout shift from photos.

Deliver 320px/768px/1440px annotated designs for home search, operation-hub search and the card family in both languages. Include sale/rental/temporary-rental examples, native currency states, missing photo, long title and verified-agent cases using clearly marked sample data. Supply a component-level implementation plan, exact files to change, copy keys, image/performance considerations and Playwright acceptance scenarios. Success: visitors understand currency and core facts, search preserves intent into working results, card links and overlays remain accessible, mobile has no overflow and images do not shift layout. Required verification plan: npm run typecheck, npm run verify:facets, npm run verify:i18n, npm run verify:seo, plus runtime responsive and keyboard checks. Do not run next build/verify:local in the constrained sandbox. Clearly separate design hypotheses from verified business outcomes.
```
