{
  "name": "inbound-email-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Email Worker: parses mail Email Routing hands it and POSTs it, signed, to the portal's /api/inbound-email. Deployed by hand with wrangler (README.md) — never from CI.",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "postal-mime": "^2.7.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260926.1",
    "typescript": "^5.8.3",
    "wrangler": "^4.141.0"
  }
}
