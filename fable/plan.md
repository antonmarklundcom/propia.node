# fable/plan.md — fix-and-harden plan for propia.node

Written 2026-09-02 from `fable/REVIEW.md`. Executed by Opus and Sonnet
sessions; Fable is never an executor of any phase here.

**Relationship to the existing plan.** `PLAN.md` at the repo root stays the
product tracker (milestones, founder decisions D1–D21, the build batches, the
migration runbook). This file supersedes only its "Build batches for the
builder chats" section for the items below; everything gated on a blank
founder decision (D3, D5, D9, D10) stays parked in `PLAN.md` and is repeated in
§8 here so no session builds it by accident. `CLAUDE.md` beats both files on
conventions.

## Phase table

| Phase | Model | Prompt file | Plan sections | Merge rule |
| --- | --- | --- | --- | --- |
| O1 request-path hardening | Opus | `fable/prompts/opus-1-request-path.md` | §5.1 | merge when green (no auth, no schema) |
| O2 public-write throttles | Opus | `fable/prompts/opus-2-write-throttles.md` | §5.2 | **open PR, do not merge** — auth surface, founder flags first |
| S1 docs, ops and build truth | Sonnet | `fable/prompts/sonnet-1-docs-ops.md` | §6.1 | merge when green |
| S2 i18n and OG on profile/category pages | Sonnet | `fable/prompts/sonnet-2-i18n-og.md` | §6.2 | merge when green; last phase, STOP |

Order is strict: O1 → O2 → S1 → S2. No phase touches `src/db/schema.ts`, so
no migration is generated by this plan.

## §1 Decisions already made — do not re-litigate

From the repo's own rules (CLAUDE.md, PLAN.md "Standing rules"):

1. One repo, one deployment, one database. Never fork.
2. No files under `.github/workflows/`. CI is `.githooks/pre-push`
   (`npm run verify:local`).
3. Never edit `drizzle.config.ts`, `src/db/index.ts` or `DATABASE_URL`
   handling. Never raise the pool bounds.
4. Any `schema.ts` change is MIGRATION REQUIRED and never auto-merged. This
   plan makes none.
5. `git fetch origin main && git reset --hard origin/main` before branching.
   Branch names `claude/<slug>`.
6. `npm run verify:local` green before every push. `npm run verify:scopes` on
   anything touching `listingScopeWhere`, `panelScope` or a panel query (needs
   a localhost MySQL; if none is available, say so in the PR).
7. Autonomous merge is authorised for well-verified low-risk work (docs, copy,
   UI). Flag before merging anything touching auth, payments or schema.
8. No *propia* in anything a visitor, realtor or staff user sees. No
   placeholder contact email. `propia.com.py` is never reintroduced.
9. Visitor-facing strings live in `src/i18n/es.ts` with an `en.ts` peer in the
   same commit; read through `dict()` on the server, `getDictionary(locale)`
   in client components.
10. `dedupKey()` returns `null` without a phone. `scope_agency_id` stays
    `NOT NULL DEFAULT 0`. One planner for dry-run and commit.
11. Every `unstable_cache` tag has a writer, except `market-medians`, which is
    TTL-only because its only writer is a tsx script (REVIEW R9).

From REVIEW recommendations that need no founder input:

12. The CRM webhook push moves off the request path: it runs inside `after()`
    with a 5 s timeout. The lead row is the record; the push is a copy.
13. `createClaimedDraft` becomes one transaction, same shape as `upsert.ts`.
14. Registration and OTP requests get fixed-window limits via the existing
    `allowRequest()` helper: register 5 per IP per 10 min; OTP 5 per user per
    hour on top of the per-number cooldown. Numbers are defaults, not policy.
15. README's production sections are rewritten from CLAUDE.md's domain table
    and `package.json`'s real `cron:*` list. ARCHITECTURE.md gets a pointer,
    not a rewrite.
16. Profile and project pages get `profile` and `project` dictionary
    namespaces; the category template gets an `openGraph` block.

## §2 Roles & object model

Unchanged and not touched by this plan. Roles: `consumer` / `agent` /
`agency_admin` / `developer` / `admin` (`src/lib/auth/roles.ts`). Scope:
`EditScope` in `src/lib/listing-edit.ts`. Read `src/db/schema.ts` for the
tables; do not add to it.

## §3 Scope

Fix-and-harden only. In scope: REVIEW R1–R4, R6–R12. Out of scope: R5 until
Q1 is answered (S1 handles either answer if it is), R13/R14 (backlog), every
item in §8, and any new feature.

## §4 Autonomy protocol — copied conceptually into every prompt

1. Work until the phase's exit criteria all pass; never ask permission for
   in-plan work.
2. One PR per phase. Branch `claude/<slug>` off a freshly reset `origin/main`.
   Never start on top of an unmerged previous phase (O2 is the one phase that
   hands off with its PR open — see its prompt).
3. Minor non-blocking issues → `fable/KNOWN-ISSUES.md` (create it on first
   use), keep building.
4. Stop and ask only for: a missing credential with no graceful fallback, or
   a foundation decision (schema shape, auth model, money math) where guessing
   wrong forces a rewrite. Everything else: choose, record in §9, continue.
5. Missing env values never block: document in `.env.example`, degrade
   gracefully.
6. Every phase prompt is re-runnable: check what exists on the branch first,
   continue from the first unmet exit criterion.
7. Sonnet hard limits: no `schema.ts`, no `src/lib/auth/**`, no `src/db/**`,
   no `src/lib/crm.ts`, no `src/lib/import/**`, no change to any
   `unstable_cache` key. Workaround plus a §10 note instead.
8. **Model cost guardrail.** Fable (`claude-fable-5*`, any Mythos-class model)
   is never used for a phase, a subagent, a spawned session, a Workflow or a
   Routine. Phase tables name only Opus and Sonnet. A session that believes it
   needs Fable stops and tells Anton why; it never spawns it. Fable's only
   role was the planning conversation that produced this file.
9. **Handoff.** Only when four gates pass: PR merged (or, for O2 only, opened
   and flagged); exit checklist passed; pre-handoff audit done (re-run
   `npm run verify:local`, adversarially re-read your own diff, fix what you
   find); §9 entry committed. Then spawn the next phase as a NEW session with
   the claude-code-remote `create_session` tool: inherit environment and
   permission mode (never `plan`), set `model` to the next phase's model from
   the table (Opus or Sonnet only, never Fable — set it explicitly, never
   inherit), `prompt` exactly `Read fable/prompts/<next-file>.md in this repo
   and execute it.` Fallback without `create_session`: continue in the same
   window if the next phase uses the same model; stop and report at a model
   switch.
10. **Build log.** Before merging, append a dated 5–10 line entry to §9: phase
    + PR link, what now exists, deviations, where the next phase looks first.
    Fresh sessions orient from this file, §9 and `fable/KNOWN-ISSUES.md` only.

## §5 Opus phases

### §5.1 O1 — request-path hardening (REVIEW R1, R2, R10, R9)

Files: `src/lib/crm.ts`, `app/api/leads/route.ts`, `src/lib/import/claim-import.ts`,
`src/lib/import/jobs.ts`, `src/lib/cache.ts`, `src/lib/precios-queries.ts`,
`src/lib/valuation.ts`.

- `WebhookProvider.post()` gets `signal: AbortSignal.timeout(5_000)`; a timeout
  returns `{ ok: false, error: "timeout" }` like any other failure.
- In the lead route, `pushLead` and the `ghlContactId` update move into the
  existing `after()` callback with the alert. The response no longer reports
  `crm: crmResult.ok`; return `{ ok: true, leadId }` and update the one
  client that reads `crm` if any does (grep `crm` in `src/components`).
  `sendOtp` stays synchronous — the wizard needs its answer.
- `createClaimedDraft`: wrap insert → `syncDisplayCoords(tx, id)` →
  `insert(listingSources)` in `db.transaction()`. Keep `findExistingClaim`
  outside it.
- `rollbackImportJob`: wrap the body in one transaction. Keep the idempotent
  re-derivation as is — the transaction is defence in depth.
- Cache: both medians call sites use `CACHE_TTL.marketMedians`; a comment in
  `cache.ts` states the tag is TTL-only and why.
- Prove it: with a localhost DB, run the claim flow with a temporary throw
  before the `listingSources` insert and show no orphan listing remains;
  without a DB, say so in the PR and in §9. No pure check can see this.

Exit: `verify:local` green; a `grep -n "AbortSignal" src/lib/crm.ts` hit; the
lead route has exactly one `await` on the DB insert before `NextResponse.json`;
`claim-import.ts` and `jobs.ts` each contain `db.transaction(`; PR merged.

### §5.2 O2 — public-write throttles (REVIEW R3, R4, R11)

Files: `app/registro/actions.ts`, `app/publicar/actions.ts`,
`src/lib/rate-limit.ts` (comment only), `src/i18n/es.ts` + `en.ts` (one new
error string each if the registration page needs a "too many attempts" copy).

- `registerAction`: `allowRequest(\`register|${ip}\`, 5, 10 * 60_000)` using
  `clientIpFrom(await headers())`; on refusal redirect back with the existing
  error-query pattern (`fail(...)`) and a new `kind`/`error` value.
- `requestOtpAction`: after `requireUser`, `allowRequest(\`otp|${user.id}\`, 5,
  60 * 60_000)`; refusal returns `{ ok: false, error: "cooldown", cooldownMs }`
  so the wizard's existing cooldown UI handles it. Keep `createOtp` unchanged.
- `rate-limit.ts` comment: replace "One Node process" with the truth from
  `src/db/index.ts` (Passenger may run several; limits are per process).
- Do not touch `src/lib/auth/**` beyond reading it.

Exit: `verify:local` green; both actions call `allowRequest` before any
scrypt or DB write; **PR opened with title prefix `[auth — flag before
merge]`, not merged**; §9 entry says so; hand off to S1 from `origin/main`.

## §6 Sonnet phases

Hard limits (§4.7) apply. Data access only through existing query modules.

### §6.1 S1 — docs, ops and build truth (REVIEW R6, R12, R5 if Q1 answered)

Files: `README.md`, `ARCHITECTURE.md` (one pointer paragraph at the top of
§5 CRM strategy), `CLAUDE.md` (the safe-fetch bullet; the "Last verified"
date), `PLAN.md` (banner date; `[YOU]` cron list), `docs/audit-2026-08.md`
(F1 row), `package.json:5`, `next.config.ts:8`, and — only if Anton answered
Q1 in `fable/REVIEW.md` or in the session prompt — the package-manager files.

- README: rewrite "Hostinger production setup", "Launch blockers" and the cron
  block. Domains come from CLAUDE.md's table; the cron list is exactly the
  `cron:*` scripts in `package.json`, with the two orderings that matter
  (`seed:financing` → `cron:cuotas`; `seed:locations` → `cron:geo`) and the
  migration runbook pointer to PLAN.md "Pending migration". Replace GHL as
  "CRM of record" with the optional-webhook model from `.env.example`.
- Never introduce a placeholder email or `propia.com.py`. `grep -rn
  "propia.com.py" README.md package.json next.config.ts` must return nothing
  when done. The historical audit doc keeps its text.
- Q1 unanswered: leave the package-manager files alone and add a `fable/KNOWN-ISSUES.md`
  line quoting Q1.

Exit: `verify:local` green; the grep above empty; `npm run` lists no cron
script README does not mention; PR merged.

### §6.2 S2 — i18n and OG on profile, project and category pages (REVIEW R7, R8)

Files: `src/i18n/es.ts`, `src/i18n/en.ts`, `app/inmobiliaria/[slug]/page.tsx`,
`app/agente/[slug]/page.tsx`, `app/proyecto/[slug]/page.tsx`,
`app/[operacion]/[...segments]/page.tsx`.

- New namespaces `esProfile` / `enProfile` (breadcrumb home, nav aria-label,
  verified title, empty-state line) and `esProject` / `enProject` (stage,
  type, state labels, "Disponible", "Desarrolladora", delivery label as a
  function of a `Date` and a number-locale). Reuse `breadcrumbHome` and
  `verified` keys where the dictionary already has them instead of
  duplicating. Spanish strings stay byte-identical.
- The delivery date uses the request's number locale (`es-PY` / `en-US`) the
  way `ListingCard` does, not a literal.
- Category template: `openGraph: { title: \`${title} — ${brand}\`, description }`
  following `app/[operacion]/page.tsx:56`; paged titles keep the page number.
- `npm run verify:i18n` must fail on a deliberately removed `en` key before
  the fix is reverted — prove the gate sees the new namespaces.

Exit: `verify:local` green; `grep -n '"Inicio"\|"Verificado"\|Sin propiedades'
app/inmobiliaria app/agente` empty; `grep -n "es-PY" app/proyecto` empty; the
category route's metadata contains `openGraph`; PR merged; STOP report.

## §7 Human-inputs checklist

| Item | Needed by | Who |
| --- | --- | --- |
| `npm run db:status -- --probe` against prod; `db:migrate` if 0010/0011 pending (REVIEW Q2) | now, before any phase | Anton |
| Q1 package-manager answer | S1 | Anton |
| A localhost MySQL for `verify:scopes` and the O1 orphan check | O1 (optional, degrade to a PR note) | session |
| Merge decision on the O2 PR | after O2 | Anton |
| Rotate the MySQL and panel passwords, drop the `%` Remote MySQL grant (PLAN.md `[YOU]`) | independent | Anton |

## §8 Open business questions — parked, not build work

D3 financing rates, D4 USD→PYG, D5 featured pricing, D9 retention scope, D10
re-import vs manual edits (REVIEW Q3, recommended yes → future Opus phase with
a migration), D6 flip day, D21 site-mode switch, account capacity move off the
shared Brazil plan. None blocks O1–S2.

## §9 Build log & handoff

(empty — each phase appends before merging)

## §10 Backlog

- R13: batch the `recompute-cuotas.ts` UPDATE when inventory passes ~10 k.
- R14: ESLint in pre-push, if Anton says yes (Q4).
- Q3 / D10: `manually_edited_at` guard on re-import (MIGRATION, Opus).
- A shared rate-limit store if Passenger process count grows (R11).
- F55: hero search "all cities" / budget — not re-verified this review.
- `next/image` for the four editorial home images (F42 residue, payload is
  already small).
