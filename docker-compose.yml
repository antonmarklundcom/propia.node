# Local dev database only — production MySQL lives in Hostinger hPanel.
services:
  mysql:
    image: mysql:8.4
    environment:
      MYSQL_DATABASE: propia
      MYSQL_USER: propia
      MYSQL_PASSWORD: propia
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3306:3306"
    volumes:
      - mysql-data:/var/lib/mysql

volumes:
  mysql-data:
